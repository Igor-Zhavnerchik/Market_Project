<Page x:Class="Market_Project.Views.ProductList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      d:DataContext="{d:DesignInstance Type=local:ProductListViewModel}"
      xmlns:local="clr-namespace:Market_Project.ViewModels" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductList">


    <Page.Resources>
        <CollectionViewSource x:Key="CategoriesSource" Source="{Binding Categories}" />
        <CollectionViewSource x:Key="UnitsSource" Source="{Binding Units}" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <DataGrid 
            Style="{StaticResource BasicDataGrid}"
            Grid.Row="0"
            x:Name="dataGrid"
            
            ItemsSource="{Binding Products}" 
            SelectedItem="{Binding SelectedItem}">
            
            <DataGrid.Columns>
                
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridComboBoxColumn 
                    Header="Category"
                    SelectedValuePath="Id"
                    SelectedValueBinding="{Binding CategoryId, Mode=TwoWay}"
                    DisplayMemberPath="Name"
                    ItemsSource="{Binding Source={StaticResource CategoriesSource}}">
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}"></DataGridTextColumn>
                <DataGridComboBoxColumn 
                    Header="Units"
                    SelectedValuePath="Id"
                    SelectedValueBinding="{Binding UnitId, Mode=TwoWay}"    
                    DisplayMemberPath="Name"
                    ItemsSource="{Binding Source={StaticResource UnitsSource}}">
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}"></DataGridTextColumn>
                <DataGridTextColumn Header="Discount" Binding="{Binding Discount}"></DataGridTextColumn>
                <DataGridCheckBoxColumn Header="Is Active" Binding="{Binding IsActive}"></DataGridCheckBoxColumn>

            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="1" Content="Save Changes" Click="Button_SaveChanges" Style="{StaticResource BasicButton}"/>
    </Grid>
</Page>
