<Window x:Class="Market_Project.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Market_Project.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
        mc:Ignorable="d"
        Title="MarketName Login Page" Height="200" Width="300" ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource FormTextblockStyle}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource FormTextBoxStyle}"/>
    </Window.Resources>
    
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="Login" Grid.Row="0" Grid.Column="0"/>
        <TextBox Text="{Binding FormLogin, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="Password" Grid.Row="1" Grid.Column="0"/>
        <TextBox Text="{Binding FormPassword, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1"/>

        <Button Content="Login" Command="{Binding LoginCheckCommand}"  Style="{StaticResource BasicButton}"  Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right"/>
    </Grid>
</Window>
